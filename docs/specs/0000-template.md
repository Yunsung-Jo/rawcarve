# [프로그램 이름]

- **날짜:** YYYY-MM-DD
- **상태:** Draft | Accepted | Superseded by [NNNN](NNNN-제목.md)

> 살아있는 문서 — 코드가 동작을 바꾸면 이 spec을 갱신한다(스냅샷이 아니다).
> 동작(what)을 기술하고, 대안 대비 선택 근거(why)는 직접 적지 말고 해당 ADR을 링크한다.
> 복잡한 프로그램은 아래 고정 절에 더해 `동작 원리(알고리즘)` 같은 절을 적응적으로 추가해도 된다.

---

## 개요

> 이 프로그램이 무엇을 하는가. 어떤 문제를 해결하는가. 누가 어떤 상황에서 사용하는가.

## 인터페이스

> 사용자가 이 프로그램을 어떻게 제어하는가.
> CLI라면 아래 형식의 옵션 테이블, GUI라면 주요 UI 요소와 동작을 기술한다.

| 이름 | 설명 | 타입 | 기본값 |
|------|------|------|--------|
| | | | |

## 출력

> 이 프로그램이 무엇을 만들어내는가. 생성되는 파일의 위치, 구조, 네이밍 규칙, 포맷을 기술한다.

## 파이프라인

> 실행 시 어떤 순서로 무슨 일이 일어나는가.
> 각 단계에서 어떤 모듈이 무엇을 하는지, 단계 간에 어떤 데이터가 흐르는지 포함한다.

1. **단계 이름** (`모듈명`) — 설명
2. **단계 이름** (`모듈명`) — 설명

## 사용하는 모듈

> 이 프로그램이 의존하는 모듈 목록. 모듈을 수정하거나 삭제할 때 영향 범위 파악에 사용한다.

- `모듈 경로` — 역할

## 의존하는 포맷 / 스펙

> 이 구현이 전제로 삼는 포맷·스펙 지식은 직접 작성하지 않고 `docs/reference/`의 해당 문서를 링크로 참조한다.

- [문서 제목](../reference/파일명.md)

## 엣지 케이스

> 일반적이지 않은 입력이나 상황에서 어떻게 동작하는가.
> 처리하지 못하는 케이스(알려진 한계)도 명시한다.

| 상황 | 동작 |
|------|------|
| | |

<!-- 작성 후: ../README.md(스펙 목록)에 한 줄 추가. 작성 가이드: write-spec 스킬 -->
